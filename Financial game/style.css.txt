:root {
    --bg: #f0f4f8;
    --card-bg: #ffffff;
    --primary: #2ecc71; /* Cash Green */
    --secondary: #3498db; /* Banking Blue */
    --danger: #e74c3c; /* Debt Red */
    --text: #2c3e50;
    --gold: #f1c40f;
}

* {
    box-sizing: border-box;
    margin: 0;
    padding: 0;
}

body {
    font-family: 'Segoe UI', Roboto, Helvetica, Arial, sans-serif;
    background-color: var(--bg);
    color: var(--text);
    display: flex;
    justify-content: center;
    align-items: center;
    min-height: 100vh;
    overflow: hidden; /* Prevents scrollbars during money blasts */
}

#app-wrapper {
    width: 100%;
    max-width: 450px;
    background: var(--card-bg);
    border-radius: 24px;
    box-shadow: 0 20px 40px rgba(0,0,0,0.1);
    position: relative;
    z-index: 10;
    padding-bottom: 20px;
}

/* --- HUD Header --- */
#game-hud {
    display: flex;
    justify-content: space-between;
    padding: 20px;
    background: #fafbfc;
    border-radius: 24px 24px 0 0;
    border-bottom: 1px solid #eee;
}

.stat-box label {
    display: block;
    font-size: 0.7rem;
    font-weight: 800;
    color: #95a5a6;
    letter-spacing: 1px;
}

.stat-box div {
    font-size: 1.2rem;
    font-weight: 700;
}

.main-balance {
    text-align: center;
}

#balance-container {
    font-size: 1.8rem;
    color: var(--primary);
    transition: transform 0.2s;
}

/* --- Progress Bar --- */
.progress-track {
    height: 6px;
    background: #ecf0f1;
    width: 100%;
}

#month-progress {
    height: 100%;
    background: var(--secondary);
    transition: width 0.5s ease-in-out;
}

/* --- Game Screen & Cards --- */
#game-screen {
    padding: 30px 20px;
}

#event-card {
    text-align: center;
}

#card-icon {
    font-size: 3rem;
    margin-bottom: 15px;
}

#event-title {
    margin-bottom: 10px;
    font-size: 1.4rem;
}

#event-description {
    color: #7f8c8d;
    line-height: 1.5;
    margin-bottom: 25px;
}

/* --- Buttons --- */
#options-area {
    display: flex;
    flex-direction: column;
    gap: 12px;
}

.primary-btn, button {
    padding: 15px;
    border: none;
    border-radius: 12px;
    background: var(--secondary);
    color: white;
    font-size: 1rem;
    font-weight: 600;
    cursor: pointer;
    transition: all 0.2s;
}

.primary-btn:hover {
    filter: brightness(1.1);
    transform: translateY(-2px);
}

.option-btn {
    background: #f8f9fa;
    color: var(--text);
    border: 2px solid #edf2f7;
}

.option-btn:hover {
    background: #edf2f7;
    border-color: var(--secondary);
}

/* --- Animations & Effects --- */
.shake {
    animation: shakeAnim 0.4s ease-in-out;
}

@keyframes shakeAnim {
    0%, 100% { transform: translateX(0); }
    25% { transform: translateX(-8px); }
    75% { transform: translateX(8px); }
}

.particle {
    position: fixed;
    pointer-events: none;
    z-index: 100;
    animation: explode 1s forwards ease-out;
}

@keyframes explode {
    0% { transform: translate(0, 0) scale(1); opacity: 1; }
    100% { transform: translate(var(--x), var(--y)) rotate(360deg) scale(0); opacity: 0; }
}

/* --- Overlays --- */
.hidden { display: none !important; }

#game-over-screen {
    position: absolute;
    top: 0; left: 0; width: 100%; height: 100%;
    background: rgba(255,255,255,0.95);
    z-index: 50;
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 40px;
    text-align: center;
    border-radius: 24px;
}

#feedback-note {
    margin-top: 15px;
    padding: 10px;
    border-radius: 8px;
    font-size: 0.9rem;
    background: #fff9c4;
    border: 1px solid #fbc02d;
}

/* Background Blobs for Visual Interest */
.bg-blob, .bg-blob2 {
    position: fixed;
    width: 400px;
    height: 400px;
    background: radial-gradient(circle, rgba(46, 204, 113, 0.1) 0%, rgba(255,255,255,0) 70%);
    z-index: 1;
}
.bg-blob { top: -100px; right: -100px; }
.bg-blob2 { bottom: -100px; left: -100px; }